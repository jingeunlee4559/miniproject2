spring.application.name=backend
server.port=8090
spring.output.ansi.enabled=always

# db
spring.datasource.type=com.zaxxer.hikari.HikariDataSource
spring.datasource.driver-class-name=oracle.jdbc.driver.OracleDriver
spring.datasource.url=jdbc:oracle:thin:@localhost:1521/xepdb1
spring.datasource.username=SCOTT
spring.datasource.password=TIGER

mybatis.mapper-locations=classpath:/mapper/*.xml
mybatis.type-aliases-package=com.example.backend.model
mybatis.type-handlers-package=com.example.backend.model

# Gemini API
gemini.api.key=AIzaSyBwR5rvigm4MX1XRV4VQG_KxAy1pbss2X0
gemini.api.endpoint=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent

# image URL
file.upload-dir=C:/dev/project-images/

# 세션 쿠키의 SameSite 속성을 None으로 설정 (크로스 도메인 허용)
server.servlet.session.cookie.same-site=Lax
# 로컬 개발 환경에서 HTTP로도 쿠키가 전송되도록 Secure 속성 비활성화 (운영 환경에서는 반드시true)
server.servlet.session.cookie.secure=false